import styled from "styled-components";

export const StyledH1 = styled.h1`
  overflow: hidden;
  white-space: nowrap;

  @media (max-width: 500px) {
    transition: 0.5s;
    font-size: 30px;
  }
`;
